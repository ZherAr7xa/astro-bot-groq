[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "astro-bot"
version = "0.1.0"
dependencies = [
    "aiogram==2.25.1",
    "requests==2.31.0",
    "python-dotenv==1.0.1"
]

[build-options]
--no-binary = aiohttp
